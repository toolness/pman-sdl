/* globals.h

   Put all global defines that other include files depend on here.
*/

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif

/* Uncomment this to compile audio support and sound effects into the game. */
//#define USE_AUDIO 1

/* Uncomment this to deactivate all the assert() statements in the game. (If this
   directive is used, the assert() statements are not processed.) */
//#define NDEBUG
